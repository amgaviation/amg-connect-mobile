import { PlaceholderScreen } from "@/components/layout/placeholder-screen";

export default function RequestsTab() {
  return (
    <PlaceholderScreen
      eyebrow="Requests"
      title="Support requests"
      description="Support requests will appear here once connected to AMG Connect."
      notes={["Request intake, review, quoting, coordination, and closeout status are deferred to later build phases."]}
    />
  );
}
